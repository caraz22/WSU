int confirm() {
    printf("Please enter 'y' or 'n': ");
    char isKeyPressed = 0;
    char character = 0;
    // while (!isKeyPressed) {
    //     char isKeyboardReady = 0;

        // while (!isKeyboardReady) {
        //     uint8 status = inb(0x64);
        //     status &= 0x01;
        //     isKeyboardReady = status == 1;
        // }
        // uint8 scancode = inb(0x60);

    //     isKeyPressed = (scancode & 0x80) == 0 && scancode < 128;

    //     if (!isKeyPressed && scancode == 0x31 || scancode == 0x15) {
    //         if (scancode == 0x31) {
    //             continue;
    //             return 0;
    //         } else if (scancode == 0x15) {
    //             continue;
    //             return 1;
    //         } else {
    //             printf("Invalid character pressed.");
    //         }
    //     }
    // }
}