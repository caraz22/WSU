public class ExamPrep {

    public static void main(String[] args) {

        //int x = Integer.MAX_VALUE;
        long x = Integer.MAX_VALUE;
        System.out.println(x);
        System.out.println(++x);
        //overflow is when the number is too big to fit the datatype
        //number will turn negative when it should be positive
        //solution: change x from int to long

        /*
        int x = 42;
        double y = x;
        //typcasting can be used for storing a double as an int
        double a = 3.14;
        int b = (int) a;
        
        ++x; //pre-increment
        x++; //post-increment
        

        long c = x;
        short d = (short) x; //typecasting is required

        if (5 < 10 || x > y) {
            //shortcircuiting because 5 < 10 is true and it's an or statement
            //so it doesnt need to check for the other condition
        }
        */

    }
    
}
