//import java.util.Scanner;

class BMI {

	public static void main(String[] args) {
		/*
		int weight = getIntValue("Enter your weight in pounds: ");
		int height = getIntValue("Enter your height in inches: ");
		double bmi = computeBMI(weight, height);
		displayBMI(bmi);
	}

	// display the prompt to the user and read in and return an int
	public static int getIntValue(String prompt) {
		Scanner in = new Scanner(System.in);
		System.out.print(prompt);
		return in.nextInt();
	}

	// weight / height^2 x 703
	public static double computeBMI(int weight, int height) {
		return weight / Math.pow(height, 2) * 703;
	}

	// round to one decimal place
	public static void displayBMI(double bmi) {
		System.out.printf("Your BMI is %.1f\n", bmi);
	*/
	}
}
