#include <iostream>

using namespace std;

void startPlaying(const Playlist &);

int main() {
    Playlist myPlaylist;
    startPlaying(myPlaylist)
}

void startPlaying(const Playlist & theList) {
    // Code for startPlaying
}